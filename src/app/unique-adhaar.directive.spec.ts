import { UniqueAdhaarDirective } from './unique-adhaar.directive';

describe('UniqueAdhaarDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueAdhaarDirective();
    expect(directive).toBeTruthy();
  });
});
